# Known Issues

## Bootstrap probe file

`_seed_probe.tmp` was created during repository bootstrap to verify write access.

It is not part of the Loop Engineering Agent Pack. It is safe to delete manually from GitHub or from a local clone.

The GitHub delete action available in this chat was blocked by a safety layer, so the file was left in place with an explanatory note instead of being removed automatically.
